{
  "name": "cycle-css-driver",
  "version": "0.0.1",
  "description": "A Cycle.js Driver for loading CSS asynchronously through filamentgroup/loadCSS",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/yagniio/cycle-css-driver"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./lib",
    "compile": "npm run clean && babel --presets es2015 -d lib/ src/",
    "watch": "npm run clean && babel --presets es2015 --watch -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "author": "Michael Bearne",
  "license": "MIT",
  "peerDependencies": {
    "xstream": ">=5"
  },
  "dependencies": {
    "@cycle/xstream-adapter": "^3.0.2",
    "fg-loadcss": "^1.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "xstream": "^5.1.2"
  }
}
